// const hobbies0 = ['sport', 'cooking', 'traveling', 'reading'];

// // const hobby1 = hobbies[0];
// // const hobby2 = hobbies[1];

// const [hobby1, hobby2, ...remainings] = hobbies0;

// console.log(hobby1);
// console.log(hobby2);
// console.log(remainings);

// const person = {
//   personName: 'zack',
//   age: 35,
//   hobbies: ['swimming', 'sport'],
// };

// const { personName: userName, age, hobbies } = person;

// console.log(userName);
// console.log(age);
// console.log(hobbies);

/**
 * we can destructure an array or an object
 * the values we give to the array values are given to real array values indexes, we can even use the spread operator to get the remainings values
 * in both destructions the original array or object are not changed after the destruction
 * diferences:
 * the values are pulled out from the array in order,because are pulled by position and the array is an oredered list
 * the order is not guaranted in object destruction because the values are pulled by keyname
 */
