// define function type with custom type
// type AddFn = (a: number, b: number) => number;

// let add: AddFn;

// add = (a, b) => a + b;
// console.log(add(5, 6));

// define function type with interface
// interface AddFnInterface {
//   (a: number, b: number): number;
// }

// let addInterf: AddFnInterface;

// addInterf = (a, b) => a + b;

// console.log(addInterf(7, 3));

/**
 * we have said that interfaces are used to define the structure of the object
 
* interfaces can be used to define the structure of a function, because functions at the end are objects too
 
* it s abit different compared to defining methods in objects in interface where we put the name of the method greet(phrase:string):void.
 
 * using custom types with the functions is a bit common and way shorter.
 */
