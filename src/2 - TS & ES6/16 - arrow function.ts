// const add = (a: number, b: number) => {
//   return a + b;
// };

// const add = (a: number, b: number) => a + b;

// console.log(add(10, 5));

// const printOutput0 = (output: number | string) => {
//   console.log(output);
// };
// const printOutput1: (output: number | string) => void = (output) =>
//   console.log(output);

// printOutput0(add(10, 2));
// printOutput1(add(7, 7));

// const button = document.querySelector('button');

// // .2
// if (button) {
//   button.addEventListener('click', (event) => console.log(event));
// }

/**
 * with arrow funtion we can explicitely assign the types after the variable name, or inside the funtion paramters.
 * 2. with Arrow Function TS you can benefit alot from short syntax of the arrow function like within callbacks of events
 */
