// type DataSource = {
//   [key: string]: number | boolean;
// };

// let someObj: Record<string, number | boolean>;

// let store: DataSource = {};

// store.id = 5;
// store.isOpen = false;
// // store.name = 'Max'
