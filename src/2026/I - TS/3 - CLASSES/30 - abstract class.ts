// abstract class UIElement {
//   constructor(public idientifier: string) {}

//   clone(targetLocation: string) {
//     //  logic to duplicate the UI element
//   }
// }

// // let uiElement = new UIElement() // // does not work (cant instantiate abstract classes) - abstract classes works as blueprint base classes which other classes should be inheriting from

// class SideDrawerElement extends UIElement {
//   constructor(
//     public identifier: string,
//     public position: 'left' | 'right',
//   ) {
//     super(identifier);
//   }

//   // ...
// }
