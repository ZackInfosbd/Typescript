// const fetchedUserData = {
//   id: 'u1',
//   name: 'zack',
//   job: { title: 'CEO', description: 'My own company' },
// };

// // .1
// // console.log(fetchedUserData.job && fetchedUserData.job.title);

// // .2
// console.log(fetchedUserData?.job?.title);

/**
 * Optional chaining:
 * scenario: lets say that we re getting some fetched data from a resourse, but we dont know if in a object a certain property is defined!!!; maybe we dont fetch all the data
 * 1. optional chaining in JS
 */
