// Reverse Number

// const reversedNumber = (num: number) => {
//   const convertedNumber = num.toString().split('').map(Number);
//   const reversedNumber = convertedNumber.reverse();
//   const convertedToStr = reversedNumber.join('');
//   const convertedStrToNum = parseFloat(convertedToStr);

//   return convertedStrToNum;
// };

// console.log(reversedNumber(123456));
